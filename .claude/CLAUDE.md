# .claude/CLAUDE.md

This is a pointer file. The canonical project bible lives at the repo root:

→ [`../CLAUDE.md`](../CLAUDE.md)

Read it first. Everything in `.claude/agents/`, `.claude/skills/`, and
`.claude/rules/` extends or specializes the constraints in the root file.
